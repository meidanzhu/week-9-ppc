<h1>New Party</h1>


<% if @party.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@party.errors.count, "person") %> prohibited this party from being saved:</h2>
 
    <ul>
    <% @party.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>

<%= form_for @party do |f| %>


    <%= f.label :name %>
    <%= f.text_field :name %>
    <%= f.label :date %>
    <%= f.date_field :date %>
    <%= f.label :budget %>
    <%= f.number_field :budget %>
    <%= f.submit %>

<% end %>